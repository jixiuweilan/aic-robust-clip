# Experiment Configurations

Future experiment configurations belong here. Each configuration should state
the competition stage, dataset manifest, exact CLIP ViT-B/32 weight source,
trainable parameter subset, seed, optimization settings, and output location.
