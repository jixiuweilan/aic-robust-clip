# 两台 4060 任务入口（已更新）

2026-09-19 起，执行[4060 缓存修复与 LoRA 学习率任务包](4060-next-round.md)。本页旧的“组员 A 重跑 B03、组员 B 重跑 B04 十轮”步骤已退出当前任务，**不再重复 B04 或旧 B03 十轮基线，不重建共同 HEAD3**。

- 两台机器先使用新源码完成全套零失败、零跳过测试及两次更新的合成 CUDA 启动检查。B04 历史 WSL 缓存测试失败必须在原组员机器上复验，新日志与旧失败报告并存。
- 使用独立 `python -m aic_robust_clip.rtx4060` 入口，测量六组工程配置，选择两机均通过的共同配置，并各做三次独立验证退出检查。
- A 做 LoRA LR `1e-4` 对 `3e-5`；B 做 `1e-4` 对 `3e-4`。每人先同期对照再候选，完整三轮暂停，保留 ONLINE10 十轮计划。
- 两机新准入回执齐备后正式开跑。所有输出使用新目录，保存原始回传。分数优先，不设绝对耗时淘汰门槛。
- 不操作正在运行的 T4、不用 confirm/test、不提交排行榜；本机仅允许正确性／有界启动验证。

完整命令、前置条件、验收标准、失败停止条件和回传清单以[新任务说明](4060-next-round.md)为准。
