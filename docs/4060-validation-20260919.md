# 4060 缓存修复：本机交付回执（2026-09-19）

本轮实现已经完成，本机验证全部通过。两台 4060 的实际 CUDA 测速、原 B04 机器复验及正式实验尚未执行，不把本回执当作远端准入。

## 变更

- `data/relocation.py`：Linux inotify＋stat 的进程缓存；先监控后哈希，变更重新强校验，监控丢失／溢出／删除／替换拒绝继续，fork/spawn 隔离与退出清理。
- `data/dataset.py`：监控发现变更时同步关闭旧 ZIP 句柄，读取失败清理当前读取器。
- `rtx4060.py`：独立配置准备、测试／启动回执、两机测速选择、三次验证准入及三轮暂停入口。没有修改 T4 调度器或共享训练／测速实现。
- 更新 4060 旧任务入口、迁移说明；历史对照页面增加当前任务提示；`feedback/` 加入忽略。原始回传没有修改或删除。

接口与 manifest、split、缓存、HEAD3、检查点格式不变。FP16 与完整轮次暂停复用本轮开始前工作区已有实现；最小补丁依赖这些前置修改，不能只对旧干净 HEAD 应用后就声称版本一致。交付同时提供精确源码快照供独立 4060 目录部署；严禁部署到当前 T4 运行目录。

## 实测验证

| 检查 | 结果 |
|---|---|
| relocation 专项 | 21/21，零失败、零跳过 |
| 4060 工具专项 | 8/8，零失败、零跳过 |
| 最终完整 suite | 121/121，零失败、零跳过，21.271 秒 |
| `compileall -q src tests` | 通过 |
| `pip check` | `No broken requirements found.` |
| `git diff --check` | 通过 |

全部为 CPU 合成／fixture 软件验证，没有正式训练、比赛数据拟合、CUDA 性能测量、模型下载、SSH、远端操作或 GitHub 推送。首次沙箱内 suite 因 PyTorch worker 所需的本地 Unix socket 被禁止而中断；解除该限制后完整执行，没有跳过用例或改变测试逻辑来通过。中断日志与最终通过日志一并保留。

环境：Python `3.11.16`，Torch `2.7.0+cpu`，Transformers `4.57.6`，Pillow `11.3.0`，NumPy `2.2.6`，huggingface-hub `0.36.2`，WSL2 Linux。远端必须重新生成自己的依赖与准入回执，不能复用这些 CPU 结果。

最终源码身份（含工作区未提交源码）：

```text
ab48c9e3dd75f59476344e8ad4cec6dd342453dc+src.e4cfa015d14a6575ffa19585ce0d958c5a1db0911bc5317929b8b71cab6a7c25
```

## 交付与待执行事项

任务包位于 `outputs/4060-cache-task-20260919-v1/`，包含最小补丁、完整源码快照、逐文件源码清单、测试原始日志、机器可读回执和 `SHA256SUMS`。补丁在临时副本完成应用检查，快照按严格文件清单生成，不包含环境、数据、权重、检查点、历史回传或 `.git`。

按[中文任务说明](4060-next-round.md)执行。组员 A 为 `1e-4` 对 `3e-5`，B 为 `1e-4` 对 `3e-4`。两机先完整工程验收，B04 原失败须在原机器复验；随后每支三轮暂停、等待负责人结合本机同期增益与 T4 结果决定下一观察点。不重跑旧 B03/B04 十轮基线，不重建 HEAD3，不使用 confirm/test，不提交排行榜。
