# 复赛交付链本机验收（2026-09-22）

复赛专项 **28项通过**（33.617秒）；全套 **194项通过**（64.555秒），
均为零失败、零错误、零跳过。
compileall、pip check、bash语法和git diff --check通过。仅生成图片与随机微型模型，
不下载、不读取真实复赛图片、不执行正式训练，不能代替真实六卡准入或模型分数。

新增五项交付专项覆盖：

- 审计中断/登记摘要不符保留退出证据，不发布可训练资产，已有原件不变。
- 独立会话任务失败保留步骤日志与退出码，launch不冒充训练完成；拒绝复用输出及初赛任务格式。
- 实际路径迁移、HEAD20权重加载、外层准入、配置校验、首轮保存、恢复至第十轮和学生dev回放。
  该测试只替换硬件与官方大模型为CPU微型结构，未替换准入、配置、HEAD加载和ZIP读取链路。
- 全train合成评分后SNSCL活动路径完成两次batch128更新，队列更新，不保存权重。
- 方法数值/选择失败阻止所属准入组，即使存在其他通过网格点也不能放行。

已有错误赛段/HEAD3/旧回执拒绝、AMP跳过更新、RNG/方法状态恢复、队列与动量、
30轮调度/十轮暂停、归档缓存失效、worker生命周期测试全部保留并通过。
GPU完整UUID在运行时规范化，正式配置入口接受等价nvidia-smi前缀/大小写表示；
资产、源码、机器及依赖身份仍严格校验。

环境：Python3.11.16，torch2.7.0+cpu，transformers4.57.6，NumPy2.2.6，
Pillow11.3.0，huggingface-hub0.36.2。
最终Python源码树SHA256：`17303623231ac9fec3bfaad373c0db9093501776ac63514cc2e031d61f812fc5`。
固定Git提交由交付回执绑定；验证时父提交为8f1b238，源码包含本次待提交更改。

开发机持久证据目录：`outputs/second_round/engineering-20260922/`。
全套最终日志 `full-suite-module.log`，汇总 `acceptance.json`，环境 `environment.json`，
专项最终日志 `focused-final.log`；目录不会发布GitHub。
此前受限沙箱本地socket失败，以及用stdin作主入口导致spawn找不到主文件的失败日志
均保留。最终通过使用仓库标准 `python -m unittest discover -s tests -v` 入口，
在允许本地进程通信的环境中执行，没有跳过或删改失败测试。

交付边界：源码/组员说明发布不代表组员已开始；本机T4包不代表上传成功。
公共HEAD20、真实train可行性、六卡网格/并发准入、八支十轮及实际耗时仍待各执行人回传。
原始训练包的接收记录不等于完整审计成功；此前中断审计及全部初赛证据保持原样。
