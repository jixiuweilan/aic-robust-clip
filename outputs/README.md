# Local Outputs

Logs, predictions, reports, and submission archives are ignored by Git. Before
uploading a submission, validate it with:

```bash
PYTHONPATH=src python3 -m aic_robust_clip.submission outputs/submission.zip
```
